export enum AppRoutes {
  AUTH = 'auth',
  MAIN = 'main'
}

export enum AuthRoutes {
  LOGIN = 'login',
  REGISTER = 'register',
  TEST = 'test'
}

export enum MainRoutes {
  OVERVIEW = 'overview',
  ANALYTICS = 'analytics',
  HISTORY = 'history',
  ORDER = 'order',
  CATEGORIES = 'categories'
}